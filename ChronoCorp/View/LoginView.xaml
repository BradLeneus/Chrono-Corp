<Window x:Class="ChronoCorp.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Connexion" Height="250" Width="400">

    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="250">

            <TextBlock Text="ID Employé" Margin="0,0,0,5"/>
            <TextBox Text="{Binding EmployeeId, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"
                     PreviewTextInput="NumericOnly" />

            <TextBlock Text="Mot de passe" Margin="0,0,0,5"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,10" PasswordChanged="PasswordBox_PasswordChanged"/>

            <Button Content="Se connecter"
                    Command="{Binding LoginCommand}" Width="100" HorizontalAlignment="Center"/>

            <TextBlock Text="{Binding LoginMessage}" Foreground="Red" Margin="0,10,0,0" TextAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
