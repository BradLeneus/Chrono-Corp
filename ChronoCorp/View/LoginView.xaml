<Window x:Class="ChronoCorp.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Connexion" Height="250" Width="400">

    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="250">

            <TextBlock Text="Utilisateur" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Text="Mot de passe" Margin="0,0,0,5"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,10"/>

            <Button Content="Se connecter" Click="OnLoginClick" Width="100" HorizontalAlignment="Center"/>

            <TextBlock Text="{Binding LoginMessage}" Foreground="Red" Margin="0,10,0,0" TextAlignment="Center"/>

        </StackPanel>
    </Grid>
</Window>


