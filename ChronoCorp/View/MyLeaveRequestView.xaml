<UserControl x:Class="ChronoCorp.View.MyLeaveRequestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChronoCorp.View"
             mc:Ignorable="d" 
             HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
    <Border Style="{StaticResource ViewBorderStyle}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.5*" />
                <ColumnDefinition Width="0.5*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="0.1*"/>
                <RowDefinition Height="0.9*"/>
            </Grid.RowDefinitions>

            <TextBlock Style="{StaticResource CurrentViewTitleStyle}">Mes demandes de congés</TextBlock>
            <StackPanel Grid.Row="1">
                <TextBlock>Demandes précédentes</TextBlock>
                <DataGrid AutoGenerateColumns="False"
                        ColumnHeaderStyle="{StaticResource DataGridHeaderStyle}"
                        CellStyle="{StaticResource DataGridCellStyle}"
                        BorderBrush="{StaticResource LightBlueColor}"
                        RowStyle="{StaticResource DataGridRowStyle}"
                        GridLinesVisibility="None"
                        IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Type de la demande" Width="0.6*"/>
                        <DataGridTextColumn Header="Statut" Width="0.2*"/>
                        <DataGridTextColumn Header="Mis à jour le" Width="0.2*"/>
                    </DataGrid.Columns>
                </DataGrid>
                <Button Style="{StaticResource DarkBlueButtonStyle}" Command="{Binding OpenNewLeaveRequestViewCommand}">Nouvelle demande</Button>
            </StackPanel>
            <StackPanel Grid.Row="1" Grid.Column="1">
                <ContentControl Content="{Binding CurrentLeaveView}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
